import React from "react";
import { Text, StyleSheet } from "react-native";

const BateauScreen = () => {
  return <Text style={styles.text}>BateauScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default BateauScreen;
