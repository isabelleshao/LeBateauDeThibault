import React from "react";
import { Text, StyleSheet } from "react-native";

const RestaurantScreen = () => {
  return <Text style={styles.text}>RestaurantScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default RestaurantScreen;
