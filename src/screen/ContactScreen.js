import React from "react";
import { Text, StyleSheet } from "react-native";

const ContactScreen = () => {
  return <Text style={styles.text}>ContactScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default ContactScreen;
